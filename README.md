# Devtest

start json-server 'json-server /data/data.json' (https://github.com/typicode/json-server)
start local server - 'npm start'

## Improvements

Paginate server-side, fetch data via options, e.g.`limit=20&offset=20`
Apply sortBy in respective column headers
Place filters in show/hide expand panel

Make page fully accessible
Apply unit tests